{
  "private": true,
  "name": "agent-tars",
  "version": "0.3.0",
  "description": "Agent TARS",
  "scripts": {
    "bootstrap": "pnpm --filter \"./tarko/**\" build && pnpm --filter \"./gui-agent/**\" build && pnpm --filter \"./agent-tars/**\" build && pnpm --filter \"./omni-tars/**\" build",
    "docs": "doctoc --title '## Table of Contents' README.md",
    "clean": "pnpm run -r clean",
    "dev": "ptk d",
    "dev:core": "ptk d --packages @agent-tars/core",
    "build": "pnpm run -r build",
    "build:tarko": "pnpm --filter \"./tarko/**\" --include-dependencies build",
    "build:ui": "pnpm --filter @tarko/ui --filter @tarko/agent-ui build",
    "build:agent-tars": "pnpm --filter \"./agent-tars/**\" --include-dependencies build",
    "build:omni-tars": "pnpm --filter \"./omni-tars/**\" --include-dependencies build",
    "build:gui-agent": "pnpm --filter \"./gui-agent/**\" --include-dependencies build",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "format": "pnpm prettier --write .",
    "release": "ptk release --push-tag --build --ignore-scripts --auto-create-release-branch",
    "release:dryrun": "ptk release --push-tag --build --ignore-scripts --auto-create-release-branch --dry-run",
    "release:canary": "ptk release --canary --push-tag --build --ignore-scripts --auto-create-release-branch",
    "release:full": "ptk release --push-tag --build --ignore-scripts --create-github-release",
    "release:ai": "ptk release --push-tag --build --ignore-scripts --use-ai --provider=azure-openai --model=aws_sdk_claude37_sonnet --baseURL=AWS_CLAUDE_API_BASE_URL",
    "release:ai:full": "ptk release --push-tag --build --ignore-scripts --use-ai --provider=azure-openai --model=aws_sdk_claude37_sonnet --baseURL=AWS_CLAUDE_API_BASE_URL --create-github-release",
    "release:ai:dryrun": "ptk release --push-tag --build --ignore-scripts --use-ai --provider=azure-openai --model=aws_sdk_claude37_sonnet --baseURL=AWS_CLAUDE_API_BASE_URL --dry-run",
    "github-release": "ptk github-release",
    "github-release:dryrun": "ptk github-release --dry-run",
    "changelog": "ptk changelog",
    "patch": "ptk patch"
  },
  "engines": {
    "node": ">=22",
    "pnpm": "9"
  },
  "devDependencies": {
    "@tarko/pnpm-toolkit": "workspace:*",
    "@types/node": "22.15.30",
    "@vitest/coverage-v8": "3.2.4",
    "doctoc": "2.2.1",
    "prettier": "3.5.3",
    "typescript": "5.8.3",
    "vitest": "3.2.4",
    "yaml": "2.8.0",
    "tsx": "^4.19.2"
  }
}
